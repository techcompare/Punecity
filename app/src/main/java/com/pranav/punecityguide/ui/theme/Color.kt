package com.pranav.punecityguide.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════════════
// CostPilot Premium Color Palette
// ═══════════════════════════════════════════════════════════

// Primary Brand
val CostPilotCyan = Color(0xFF00D4FF)       // Electric Cyan — CTAs, highlights
val CostPilotCyanDark = Color(0xFF00A3CC)   // Darker cyan variant
val CostPilotNavy = Color(0xFF0A1628)       // Deep Navy — backgrounds
val CostPilotNavyLight = Color(0xFF121B2E)  // Card surfaces on dark

// Accent 
val CostPilotGold = Color(0xFFFFB800)       // Warm Gold — badges, premium
val CostPilotGoldLight = Color(0xFFFFD54F)  // Light gold highlights

// Semantic
val CostPilotSuccess = Color(0xFF00E676)    // Mint Green — savings
val CostPilotDanger = Color(0xFFFF5252)     // Coral Red — over-budget
val CostPilotWarning = Color(0xFFFFAB40)    // Amber — warnings

// Text
val CostPilotSilver = Color(0xFFE8ECF0)    // Silver text on dark
val CostPilotMuted = Color(0xFF8899AA)      // Muted secondary text

// Category Colors (for expenses & comparisons)
val CatFood = Color(0xFFFF6B6B)         // Warm Red
val CatTransport = Color(0xFF4ECDC4)    // Teal
val CatAccommodation = Color(0xFF45B7D1) // Sky Blue
val CatEntertainment = Color(0xFFF7B731) // Golden
val CatShopping = Color(0xFFA55EEA)     // Purple
val CatServices = Color(0xFF26DE81)     // Green
val CatDrinks = Color(0xFFFC5C65)       // Rose
val CatOthers = Color(0xFF778CA3)       // Slate

// Gradient presets
val GradientPrimary = listOf(Color(0xFF00D4FF), Color(0xFF0066FF))
val GradientGold = listOf(Color(0xFFFFB800), Color(0xFFFF8C00))
val GradientSuccess = listOf(Color(0xFF00E676), Color(0xFF00B248))
val GradientDanger = listOf(Color(0xFFFF5252), Color(0xFFD32F2F))
val GradientPremium = listOf(Color(0xFF667EEA), Color(0xFF764BA2))